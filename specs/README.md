# Focused specifications

Create a focused spec only when behavior spans several modules and cannot be owned clearly by a primary Requirement or an implementation contract. Each rule has one primary owner; focused specs reference Requirement and ADR IDs and do not duplicate Work state or release evidence.

Current v2 preparation documents remain under `../openspec/changes/aivo-v2/` until accepted Work promotes their decisions into current primary specs.
